build:
  hello-world:
    context: .

deploy:
  - envsubst < k8s.yml | kubectl apply -f -

dev:
  hello-world:
    image: ghcr.io/okteto/rust:1
    command: bash
    sync:
      - .:/usr/src/app
    forward:
      - 2345:2345
